# Changelog

## 0.1.0

- Initial release.
- `MikaCredoRules.NoApplicationEnvOutsideConfig` — flags any read or write of
  application env outside a config module. Configurable via `:config_files` and
  `:functions`.
